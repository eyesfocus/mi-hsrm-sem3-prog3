#include <stdio.h>

double pow(double x, double n){
  double xx = x;

  if (n = 1){
    return xx;
  }

  while(n > 1){
    xx = xx*x;
    n = n-1;
  }
  return xx;
}

int main(int argc, char *argv[]){
	double eps;
	double ai;
	double ai_1;

	eps = 0.00000000000001;
	diff = eps*2;

	while (diff <= eps){
	  ai_1 = ai - 
	}
	
}
